# useSignInWithGoogle surfaces the missing-credentials error.
steps:
  - run: subflows/open-app
  - tap: { id: google-sign-in-button }
  - await: { visible: { text: Google Sign-In credentials not found }, timeout: 15000 }
