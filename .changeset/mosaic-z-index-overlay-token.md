---
'@clerk/mosaic': minor
---

Render `UserButton`'s popover and menu above positioned host content, such as a sticky header with a `z-index`. The layer defaults to `z-index: 10000` and can be changed by overriding the new `--cl-z-index-overlay` custom property.
