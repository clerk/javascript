---
'@clerk/mosaic': patch
---

Derive the `UserButton` header layout from its actions: labelled buttons stack under the workspace, and a lone gear sits inline. Header and trigger avatars share one size, and long names truncate instead of wrapping.
