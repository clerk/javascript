---
'@clerk/i18n': minor
---

Add `@clerk/i18n` — a reactive i18n engine built on nanostores. Provides `createI18n`, `params`, `count`, `messageFormat`, `defineLocalization`, and React bindings (`useStore`, `Message`).
