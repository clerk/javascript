---
'@clerk/expo': major
---

Rename the trusted-device API to biometric credentials. Use `useBiometricCredentials()` and the `BiometricCredential` types for enrollment, management, and biometric sign-in.
