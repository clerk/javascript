---
'@clerk/clerk-js': patch
---

Support injected OAuth transports in future-style sign-in and sign-up SSO flows. The configured callback is reconciled before the operation completes, while session finalization remains explicit.
