---
'@clerk/ui': patch
---

Direction-aware icons in Mosaic (the forward chevrons on menu rows and the previous/next arrows in paginated lists) now mirror horizontally when rendered under an ancestor with `dir="rtl"`.
