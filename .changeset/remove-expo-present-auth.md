---
'@clerk/expo': patch
---

Remove unused Expo prebuilt-view bridge code: the native `presentAuth` bridge, the Android auth activity/factory path, and stale `Inline*` source files now superseded by `AuthView` and `UserProfileView`.
