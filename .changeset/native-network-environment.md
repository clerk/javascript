---
'@clerk/shared': patch
'@clerk/clerk-js': patch
---

Allow native hosts to supply network availability without browser globals. Preserve existing browser and server behavior when no native host is installed.
