---
'@clerk/mosaic': minor
---

Add a `Tooltip` component with `Root`, `Trigger`, `Popup`, and `Group` parts. Tooltips open on hover or focus, render an inverted surface with small type, and wrap long labels at a 20rem max width. Wrapping neighbouring tooltips in `Tooltip.Group` shares one open delay so moving between triggers switches labels instantly.
