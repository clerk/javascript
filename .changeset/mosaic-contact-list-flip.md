---
'@clerk/ui': patch
---

The Mosaic `UserProfile` email and phone number lists now show the primary entry first, and a newly set primary slides to the top of the list instead of jumping. The slide is skipped under `prefers-reduced-motion`.
