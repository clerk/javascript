---
'@clerk/mosaic': patch
---

A `UserButton` account avatar with an organization nested on it now cuts a notch out around the organization mark, so a gap of background separates the two. An avatar-only trigger draws its focus ring around the avatar itself rather than its box.
