---
'@clerk/clerk-js': patch
'@clerk/shared': patch
---

Default organization creation advisories to warning severity when the API response omits it, so organization defaults remain usable through typed clients.
