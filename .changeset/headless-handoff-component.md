---
'@clerk/react': patch
---

Add a public headless `<Handoff />` component that consumes Clerk ticket handoff URLs, activates completed sessions, and routes incomplete flows to the configured auth pages.
