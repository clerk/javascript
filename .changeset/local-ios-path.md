---
"@clerk/expo": patch
---

Add opt-in Expo prebuild overrides for using local `clerk-ios` and `clerk-android` checkouts via environment variables, and fix Android native component resolution for the native Clerk views.
