/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersSetProfileImage } from '../../funcs/usersSetProfileImage.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.SetUserProfileImageRequest$inboundSchema,
};

export const tool$usersSetProfileImage: ToolDefinition<typeof args> = {
  name: 'users-set-profile-image',
  description: `Set user profile image

Update a user's profile image`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersSetProfileImage(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
