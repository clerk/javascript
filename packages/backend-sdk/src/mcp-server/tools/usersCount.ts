/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersCount } from '../../funcs/usersCount.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.GetUsersCountRequest$inboundSchema,
};

export const tool$usersCount: ToolDefinition<typeof args> = {
  name: 'users-count',
  description: `Count users

Returns a total count of all users that match the given filtering criteria.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersCount(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
