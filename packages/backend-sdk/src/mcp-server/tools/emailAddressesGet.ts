/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { emailAddressesGet } from '../../funcs/emailAddressesGet.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.GetEmailAddressRequest$inboundSchema,
};

export const tool$emailAddressesGet: ToolDefinition<typeof args> = {
  name: 'email-addresses-get',
  description: `Retrieve an email address

Returns the details of an email address.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await emailAddressesGet(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
