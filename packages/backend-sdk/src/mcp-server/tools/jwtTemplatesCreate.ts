/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { jwtTemplatesCreate } from '../../funcs/jwtTemplatesCreate.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.CreateJWTTemplateRequestBody$inboundSchema.optional(),
};

export const tool$jwtTemplatesCreate: ToolDefinition<typeof args> = {
  name: 'jwt-templates-create',
  description: `Create a JWT template

Create a new JWT template`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await jwtTemplatesCreate(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
