/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { emailSMSTemplatesList } from '../../funcs/emailSMSTemplatesList.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.GetTemplateListRequest$inboundSchema,
};

export const tool$emailSMSTemplatesList: ToolDefinition<typeof args> = {
  name: 'email-SMS-templates-list',
  description: `List all templates

Returns a list of all templates.
The templates are returned sorted by position.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await emailSMSTemplatesList(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
