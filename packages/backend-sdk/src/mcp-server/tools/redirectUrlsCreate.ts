/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { redirectUrlsCreate } from '../../funcs/redirectUrlsCreate.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.CreateRedirectURLRequestBody$inboundSchema.optional(),
};

export const tool$redirectUrlsCreate: ToolDefinition<typeof args> = {
  name: 'redirect-urls-create',
  description: `Create a redirect URL

Create a redirect URL`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await redirectUrlsCreate(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
