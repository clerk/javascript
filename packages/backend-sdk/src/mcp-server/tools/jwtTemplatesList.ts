/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { jwtTemplatesList } from '../../funcs/jwtTemplatesList.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.ListJWTTemplatesRequest$inboundSchema,
};

export const tool$jwtTemplatesList: ToolDefinition<typeof args> = {
  name: 'jwt-templates-list',
  description: `List all templates`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await jwtTemplatesList(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
