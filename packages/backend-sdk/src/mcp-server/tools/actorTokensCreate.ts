/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { actorTokensCreate } from '../../funcs/actorTokensCreate.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.CreateActorTokenRequestBody$inboundSchema.optional(),
};

export const tool$actorTokensCreate: ToolDefinition<typeof args> = {
  name: 'actor-tokens-create',
  description: `Create actor token

Create an actor token that can be used to impersonate the given user.
The \`actor\` parameter needs to include at least a "sub" key whose value is the ID of the actor (impersonating) user.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await actorTokensCreate(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
