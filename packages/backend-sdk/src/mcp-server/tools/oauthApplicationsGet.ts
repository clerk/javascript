/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oauthApplicationsGet } from '../../funcs/oauthApplicationsGet.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.GetOAuthApplicationRequest$inboundSchema,
};

export const tool$oauthApplicationsGet: ToolDefinition<typeof args> = {
  name: 'oauth-applications-get',
  description: `Retrieve an OAuth application by ID

Fetches the OAuth application whose ID matches the provided \`id\` in the path.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oauthApplicationsGet(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
