/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { allowlistIdentifiersCreate } from '../../funcs/allowlistIdentifiersCreate.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.CreateAllowlistIdentifierRequestBody$inboundSchema.optional(),
};

export const tool$allowlistIdentifiersCreate: ToolDefinition<typeof args> = {
  name: 'allowlist-identifiers-create',
  description: `Add identifier to the allow-list

Create an identifier allowed to sign up to an instance`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await allowlistIdentifiersCreate(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
