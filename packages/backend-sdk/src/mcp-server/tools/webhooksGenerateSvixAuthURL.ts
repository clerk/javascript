/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksGenerateSvixAuthURL } from '../../funcs/webhooksGenerateSvixAuthURL.js';
import { formatResult, ToolDefinition } from '../tools.js';

export const tool$webhooksGenerateSvixAuthURL: ToolDefinition = {
  name: 'webhooks-generate-svix-auth-URL',
  description: `Create a Svix Dashboard URL

Generate a new url for accessing the Svix's management dashboard for that particular instance`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await webhooksGenerateSvixAuthURL(client, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
