/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { samlConnectionsUpdate } from '../../funcs/samlConnectionsUpdate.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.UpdateSAMLConnectionRequest$inboundSchema,
};

export const tool$samlConnectionsUpdate: ToolDefinition<typeof args> = {
  name: 'saml-connections-update',
  description: `Update a SAML Connection

Updates the SAML Connection whose ID matches the provided \`id\` in the path.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await samlConnectionsUpdate(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
