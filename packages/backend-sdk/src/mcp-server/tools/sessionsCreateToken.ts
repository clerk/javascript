/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionsCreateToken } from '../../funcs/sessionsCreateToken.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.CreateSessionTokenRequest$inboundSchema,
};

export const tool$sessionsCreateToken: ToolDefinition<typeof args> = {
  name: 'sessions-create-token',
  description: `Create a session token

Creates a session JSON Web Token (JWT) based on a session.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionsCreateToken(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
