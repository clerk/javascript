/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oauthApplicationsList } from '../../funcs/oauthApplicationsList.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.ListOAuthApplicationsRequest$inboundSchema,
};

export const tool$oauthApplicationsList: ToolDefinition<typeof args> = {
  name: 'oauth-applications-list',
  description: `Get a list of OAuth applications for an instance

This request returns the list of OAuth applications for an instance.
Results can be paginated using the optional \`limit\` and \`offset\` query parameters.
The OAuth applications are ordered by descending creation date.
Most recent OAuth applications will be returned first.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oauthApplicationsList(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
