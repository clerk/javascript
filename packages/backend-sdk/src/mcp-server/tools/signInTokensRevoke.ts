/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signInTokensRevoke } from '../../funcs/signInTokensRevoke.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.RevokeSignInTokenRequest$inboundSchema,
};

export const tool$signInTokensRevoke: ToolDefinition<typeof args> = {
  name: 'sign-in-tokens-revoke',
  description: `Revoke the given sign-in token

Revokes a pending sign-in token`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signInTokensRevoke(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
