/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersDeletePasskey } from '../../funcs/usersDeletePasskey.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.UserPasskeyDeleteRequest$inboundSchema,
};

export const tool$usersDeletePasskey: ToolDefinition<typeof args> = {
  name: 'users-delete-passkey',
  description: `Delete a user passkey

Delete the passkey identification for a given user and notify them through email.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersDeletePasskey(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
