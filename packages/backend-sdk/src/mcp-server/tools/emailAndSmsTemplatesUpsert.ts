/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { emailAndSmsTemplatesUpsert } from '../../funcs/emailAndSmsTemplatesUpsert.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.UpsertTemplateRequest$inboundSchema,
};

export const tool$emailAndSmsTemplatesUpsert: ToolDefinition<typeof args> = {
  name: 'email-and-sms-templates-upsert',
  description: `Update a template for a given type and slug

Updates the existing template of the given type and slug`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await emailAndSmsTemplatesUpsert(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
