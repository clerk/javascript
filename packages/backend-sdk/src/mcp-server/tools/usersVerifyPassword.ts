/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersVerifyPassword } from '../../funcs/usersVerifyPassword.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.VerifyPasswordRequest$inboundSchema,
};

export const tool$usersVerifyPassword: ToolDefinition<typeof args> = {
  name: 'users-verify-password',
  description: `Verify the password of a user

Check that the user's password matches the supplied input.
Useful for custom auth flows and re-verification.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersVerifyPassword(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
