/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { redirectUrlsGet } from '../../funcs/redirectUrlsGet.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.GetRedirectURLRequest$inboundSchema,
};

export const tool$redirectUrlsGet: ToolDefinition<typeof args> = {
  name: 'redirect-urls-get',
  description: `Retrieve a redirect URL

Retrieve the details of the redirect URL with the given ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await redirectUrlsGet(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
