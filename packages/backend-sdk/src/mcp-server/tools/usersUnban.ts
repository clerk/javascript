/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersUnban } from '../../funcs/usersUnban.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.UnbanUserRequest$inboundSchema,
};

export const tool$usersUnban: ToolDefinition<typeof args> = {
  name: 'users-unban',
  description: `Unban a user

Removes the ban mark from the given user.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersUnban(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
