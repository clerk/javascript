/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { phoneNumbersCreate } from '../../funcs/phoneNumbersCreate.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.CreatePhoneNumberRequestBody$inboundSchema.optional(),
};

export const tool$phoneNumbersCreate: ToolDefinition<typeof args> = {
  name: 'phone-numbers-create',
  description: `Create a phone number

Create a new phone number`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await phoneNumbersCreate(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
