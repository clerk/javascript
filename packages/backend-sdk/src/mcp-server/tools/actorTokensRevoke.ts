/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { actorTokensRevoke } from '../../funcs/actorTokensRevoke.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.RevokeActorTokenRequest$inboundSchema,
};

export const tool$actorTokensRevoke: ToolDefinition<typeof args> = {
  name: 'actor-tokens-revoke',
  description: `Revoke actor token

Revokes a pending actor token.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await actorTokensRevoke(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
