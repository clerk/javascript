/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { signUpsUpdate } from '../../funcs/signUpsUpdate.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.UpdateSignUpRequest$inboundSchema,
};

export const tool$signUpsUpdate: ToolDefinition<typeof args> = {
  name: 'sign-ups-update',
  description: `Update a sign-up

Update the sign-up with the given ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await signUpsUpdate(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
