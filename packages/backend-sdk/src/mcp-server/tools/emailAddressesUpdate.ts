/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { emailAddressesUpdate } from '../../funcs/emailAddressesUpdate.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.UpdateEmailAddressRequest$inboundSchema,
};

export const tool$emailAddressesUpdate: ToolDefinition<typeof args> = {
  name: 'email-addresses-update',
  description: `Update an email address

Updates an email address.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await emailAddressesUpdate(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
