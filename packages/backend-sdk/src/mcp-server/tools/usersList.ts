/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersList } from '../../funcs/usersList.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.GetUserListRequest$inboundSchema,
};

export const tool$usersList: ToolDefinition<typeof args> = {
  name: 'users-list',
  description: `List all users

Returns a list of all users.
The users are returned sorted by creation date, with the newest users appearing first.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersList(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
