/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsMergeMetadata } from '../../funcs/organizationsMergeMetadata.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.MergeOrganizationMetadataRequest$inboundSchema,
};

export const tool$organizationsMergeMetadata: ToolDefinition<typeof args> = {
  name: 'organizations-merge-metadata',
  description: `Merge and update metadata for an organization

Update organization metadata attributes by merging existing values with the provided parameters.
Metadata values will be updated via a deep merge.
Deep meaning that any nested JSON objects will be merged as well.
You can remove metadata keys at any level by setting their value to \`null\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationsMergeMetadata(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
