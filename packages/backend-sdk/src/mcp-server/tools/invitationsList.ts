/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invitationsList } from '../../funcs/invitationsList.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.ListInvitationsRequest$inboundSchema,
};

export const tool$invitationsList: ToolDefinition<typeof args> = {
  name: 'invitations-list',
  description: `List all invitations

Returns all non-revoked invitations for your application, sorted by creation date`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await invitationsList(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
