/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersUnlock } from '../../funcs/usersUnlock.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.UnlockUserRequest$inboundSchema,
};

export const tool$usersUnlock: ToolDefinition<typeof args> = {
  name: 'users-unlock',
  description: `Unlock a user

Removes the lock from the given user.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersUnlock(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
