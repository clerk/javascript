/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { blocklistIdentifiersList } from '../../funcs/blocklistIdentifiersList.js';
import { formatResult, ToolDefinition } from '../tools.js';

export const tool$blocklistIdentifiersList: ToolDefinition = {
  name: 'blocklist-identifiers-list',
  description: `List all identifiers on the block-list

Get a list of all identifiers which are not allowed to access an instance`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await blocklistIdentifiersList(client, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
