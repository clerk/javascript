/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { betaFeaturesUpdateProductionInstanceDomain } from '../../funcs/betaFeaturesUpdateProductionInstanceDomain.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.UpdateProductionInstanceDomainRequestBody$inboundSchema.optional(),
};

export const tool$betaFeaturesUpdateProductionInstanceDomain: ToolDefinition<typeof args> = {
  name: 'beta-features-update-production-instance-domain',
  description: `Update production instance domain

Change the domain of a production instance.

Changing the domain requires updating the [DNS records](https://clerk.com/docs/deployments/overview#dns-records) accordingly, deploying new [SSL certificates](https://clerk.com/docs/deployments/overview#deploy), updating your Social Connection's redirect URLs and setting the new keys in your code.

WARNING: Changing your domain will invalidate all current user sessions (i.e. users will be logged out). Also, while your application is being deployed, a small downtime is expected to occur.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await betaFeaturesUpdateProductionInstanceDomain(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
