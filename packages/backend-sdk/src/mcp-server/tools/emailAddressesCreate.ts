/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { emailAddressesCreate } from '../../funcs/emailAddressesCreate.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.CreateEmailAddressRequestBody$inboundSchema.optional(),
};

export const tool$emailAddressesCreate: ToolDefinition<typeof args> = {
  name: 'email-addresses-create',
  description: `Create an email address

Create a new email address`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await emailAddressesCreate(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
