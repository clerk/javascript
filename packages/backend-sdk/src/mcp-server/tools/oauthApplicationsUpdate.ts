/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oauthApplicationsUpdate } from '../../funcs/oauthApplicationsUpdate.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.UpdateOAuthApplicationRequest$inboundSchema,
};

export const tool$oauthApplicationsUpdate: ToolDefinition<typeof args> = {
  name: 'oauth-applications-update',
  description: `Update an OAuth application

Updates an existing OAuth application`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oauthApplicationsUpdate(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
