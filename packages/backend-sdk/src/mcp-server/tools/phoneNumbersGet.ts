/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { phoneNumbersGet } from '../../funcs/phoneNumbersGet.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.GetPhoneNumberRequest$inboundSchema,
};

export const tool$phoneNumbersGet: ToolDefinition<typeof args> = {
  name: 'phone-numbers-get',
  description: `Retrieve a phone number

Returns the details of a phone number`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await phoneNumbersGet(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
