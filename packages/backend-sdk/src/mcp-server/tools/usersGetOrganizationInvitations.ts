/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersGetOrganizationInvitations } from '../../funcs/usersGetOrganizationInvitations.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.UsersGetOrganizationInvitationsRequest$inboundSchema,
};

export const tool$usersGetOrganizationInvitations: ToolDefinition<typeof args> = {
  name: 'users-get-organization-invitations',
  description: `Retrieve all invitations for a user

Retrieve a paginated list of the user's organization invitations`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersGetOrganizationInvitations(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
