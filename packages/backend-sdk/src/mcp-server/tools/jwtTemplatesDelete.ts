/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { jwtTemplatesDelete } from '../../funcs/jwtTemplatesDelete.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.DeleteJWTTemplateRequest$inboundSchema,
};

export const tool$jwtTemplatesDelete: ToolDefinition<typeof args> = {
  name: 'jwt-templates-delete',
  description: `Delete a Template`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await jwtTemplatesDelete(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
