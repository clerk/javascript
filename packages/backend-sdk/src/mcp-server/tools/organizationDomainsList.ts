/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationDomainsList } from '../../funcs/organizationDomainsList.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.ListOrganizationDomainsRequest$inboundSchema,
};

export const tool$organizationDomainsList: ToolDefinition<typeof args> = {
  name: 'organization-domains-list',
  description: `Get a list of all domains of an organization.

Get a list of all domains of an organization.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationDomainsList(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
