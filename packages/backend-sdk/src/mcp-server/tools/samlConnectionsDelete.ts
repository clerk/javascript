/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { samlConnectionsDelete } from '../../funcs/samlConnectionsDelete.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.DeleteSAMLConnectionRequest$inboundSchema,
};

export const tool$samlConnectionsDelete: ToolDefinition<typeof args> = {
  name: 'saml-connections-delete',
  description: `Delete a SAML Connection

Deletes the SAML Connection whose ID matches the provided \`id\` in the path.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await samlConnectionsDelete(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
