/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersDeleteProfileImage } from '../../funcs/usersDeleteProfileImage.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.DeleteUserProfileImageRequest$inboundSchema,
};

export const tool$usersDeleteProfileImage: ToolDefinition<typeof args> = {
  name: 'users-delete-profile-image',
  description: `Delete user profile image

Delete a user's profile image`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersDeleteProfileImage(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
