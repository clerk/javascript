/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationMembershipsDelete } from '../../funcs/organizationMembershipsDelete.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.DeleteOrganizationMembershipRequest$inboundSchema,
};

export const tool$organizationMembershipsDelete: ToolDefinition<typeof args> = {
  name: 'organization-memberships-delete',
  description: `Remove a member from an organization

Removes the given membership from the organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationMembershipsDelete(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
