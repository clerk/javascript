/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { allowlistIdentifiersDelete } from '../../funcs/allowlistIdentifiersDelete.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.DeleteAllowlistIdentifierRequest$inboundSchema,
};

export const tool$allowlistIdentifiersDelete: ToolDefinition<typeof args> = {
  name: 'allowlist-identifiers-delete',
  description: `Delete identifier from allow-list

Delete an identifier from the instance allow-list`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await allowlistIdentifiersDelete(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
