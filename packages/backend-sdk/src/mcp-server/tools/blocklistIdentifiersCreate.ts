/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { blocklistIdentifiersCreate } from '../../funcs/blocklistIdentifiersCreate.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.CreateBlocklistIdentifierRequestBody$inboundSchema.optional(),
};

export const tool$blocklistIdentifiersCreate: ToolDefinition<typeof args> = {
  name: 'blocklist-identifiers-create',
  description: `Add identifier to the block-list

Create an identifier that is blocked from accessing an instance`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await blocklistIdentifiersCreate(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
