/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { phoneNumbersDelete } from '../../funcs/phoneNumbersDelete.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.DeletePhoneNumberRequest$inboundSchema,
};

export const tool$phoneNumbersDelete: ToolDefinition<typeof args> = {
  name: 'phone-numbers-delete',
  description: `Delete a phone number

Delete the phone number with the given ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await phoneNumbersDelete(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
