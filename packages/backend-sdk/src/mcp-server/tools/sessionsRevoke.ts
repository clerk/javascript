/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionsRevoke } from '../../funcs/sessionsRevoke.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.RevokeSessionRequest$inboundSchema,
};

export const tool$sessionsRevoke: ToolDefinition<typeof args> = {
  name: 'sessions-revoke',
  description: `Revoke a session

Sets the status of a session as "revoked", which is an unauthenticated state.
In multi-session mode, a revoked session will still be returned along with its client object, however the user will need to sign in again.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionsRevoke(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
