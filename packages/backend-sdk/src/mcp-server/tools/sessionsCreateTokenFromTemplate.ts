/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionsCreateTokenFromTemplate } from '../../funcs/sessionsCreateTokenFromTemplate.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.CreateSessionTokenFromTemplateRequest$inboundSchema,
};

export const tool$sessionsCreateTokenFromTemplate: ToolDefinition<typeof args> = {
  name: 'sessions-create-token-from-template',
  description: `Create a session token from a jwt template

Creates a JSON Web Token(JWT) based on a session and a JWT Template name defined for your instance`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionsCreateTokenFromTemplate(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
