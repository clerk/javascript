/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersDeleteWeb3Wallet } from '../../funcs/usersDeleteWeb3Wallet.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.UserWeb3WalletDeleteRequest$inboundSchema,
};

export const tool$usersDeleteWeb3Wallet: ToolDefinition<typeof args> = {
  name: 'users-delete-web3-wallet',
  description: `Delete a user web3 wallet

Delete the web3 wallet identification for a given user.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersDeleteWeb3Wallet(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
