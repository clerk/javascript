/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksCreateSvixApp } from '../../funcs/webhooksCreateSvixApp.js';
import { formatResult, ToolDefinition } from '../tools.js';

export const tool$webhooksCreateSvixApp: ToolDefinition = {
  name: 'webhooks-create-svix-app',
  description: `Create a Svix app

Create a Svix app and associate it with the current instance`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await webhooksCreateSvixApp(client, { fetchOptions: { signal: ctx.signal } }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
