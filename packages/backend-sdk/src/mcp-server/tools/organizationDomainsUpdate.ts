/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationDomainsUpdate } from '../../funcs/organizationDomainsUpdate.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.UpdateOrganizationDomainRequest$inboundSchema,
};

export const tool$organizationDomainsUpdate: ToolDefinition<typeof args> = {
  name: 'organization-domains-update',
  description: `Update an organization domain.

Updates the properties of an existing organization domain.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationDomainsUpdate(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
