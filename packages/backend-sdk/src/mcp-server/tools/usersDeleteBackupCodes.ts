/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersDeleteBackupCodes } from '../../funcs/usersDeleteBackupCodes.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.DeleteBackupCodeRequest$inboundSchema,
};

export const tool$usersDeleteBackupCodes: ToolDefinition<typeof args> = {
  name: 'users-delete-backup-codes',
  description: `Disable all user's Backup codes

Disable all of a user's backup codes.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersDeleteBackupCodes(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
