/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { phoneNumbersUpdate } from '../../funcs/phoneNumbersUpdate.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.UpdatePhoneNumberRequest$inboundSchema,
};

export const tool$phoneNumbersUpdate: ToolDefinition<typeof args> = {
  name: 'phone-numbers-update',
  description: `Update a phone number

Updates a phone number`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await phoneNumbersUpdate(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
