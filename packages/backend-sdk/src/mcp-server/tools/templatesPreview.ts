/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { templatesPreview } from '../../funcs/templatesPreview.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.PreviewTemplateRequest$inboundSchema,
};

export const tool$templatesPreview: ToolDefinition<typeof args> = {
  name: 'templates-preview',
  description: `Preview changes to a template

Returns a preview of a template for a given template_type, slug and body`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await templatesPreview(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
