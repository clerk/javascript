/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { redirectUrlsDelete } from '../../funcs/redirectUrlsDelete.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.DeleteRedirectURLRequest$inboundSchema,
};

export const tool$redirectUrlsDelete: ToolDefinition<typeof args> = {
  name: 'redirect-urls-delete',
  description: `Delete a redirect URL

Remove the selected redirect URL from the whitelist of the instance`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await redirectUrlsDelete(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
