/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { jwksGetJWKS } from '../../funcs/jwksGetJWKS.js';
import { formatResult, ToolDefinition } from '../tools.js';

export const tool$jwksGetJWKS: ToolDefinition = {
  name: 'jwks-get-JWKS',
  description: `Retrieve the JSON Web Key Set of the instance

Retrieve the JSON Web Key Set of the instance`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await jwksGetJWKS(client, { fetchOptions: { signal: ctx.signal } }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
