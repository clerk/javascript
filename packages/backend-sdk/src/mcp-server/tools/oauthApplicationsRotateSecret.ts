/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oauthApplicationsRotateSecret } from '../../funcs/oauthApplicationsRotateSecret.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.RotateOAuthApplicationSecretRequest$inboundSchema,
};

export const tool$oauthApplicationsRotateSecret: ToolDefinition<typeof args> = {
  name: 'oauth-applications-rotate-secret',
  description: `Rotate the client secret of the given OAuth application

Rotates the OAuth application's client secret.
When the client secret is rotated, make sure to update it in authorized OAuth clients.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await oauthApplicationsRotateSecret(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
