/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { instanceSettingsUpdate } from '../../funcs/instanceSettingsUpdate.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.UpdateInstanceRequestBody$inboundSchema.optional(),
};

export const tool$instanceSettingsUpdate: ToolDefinition<typeof args> = {
  name: 'instance-settings-update',
  description: `Update instance settings

Updates the settings of an instance`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await instanceSettingsUpdate(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
