/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsDeleteLogo } from '../../funcs/organizationsDeleteLogo.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.DeleteOrganizationLogoRequest$inboundSchema,
};

export const tool$organizationsDeleteLogo: ToolDefinition<typeof args> = {
  name: 'organizations-delete-logo',
  description: `Delete the organization's logo.

Delete the organization's logo.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationsDeleteLogo(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
