/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersDeleteTOTP } from '../../funcs/usersDeleteTOTP.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.DeleteTOTPRequest$inboundSchema,
};

export const tool$usersDeleteTOTP: ToolDefinition<typeof args> = {
  name: 'users-delete-TOTP',
  description: `Delete all the user's TOTPs

Deletes all of the user's TOTPs.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersDeleteTOTP(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
