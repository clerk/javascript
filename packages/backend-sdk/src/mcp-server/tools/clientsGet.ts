/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { clientsGet } from '../../funcs/clientsGet.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.GetClientRequest$inboundSchema,
};

export const tool$clientsGet: ToolDefinition<typeof args> = {
  name: 'clients-get',
  description: `Get a client

Returns the details of a client.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await clientsGet(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
