/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { samlConnectionsList } from '../../funcs/samlConnectionsList.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.ListSAMLConnectionsRequest$inboundSchema,
};

export const tool$samlConnectionsList: ToolDefinition<typeof args> = {
  name: 'saml-connections-list',
  description: `Get a list of SAML Connections for an instance

Returns the list of SAML Connections for an instance.
Results can be paginated using the optional \`limit\` and \`offset\` query parameters.
The SAML Connections are ordered by descending creation date and the most recent will be returned first.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await samlConnectionsList(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
