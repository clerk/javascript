/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { clientsVerify } from '../../funcs/clientsVerify.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.VerifyClientRequestBody$inboundSchema.optional(),
};

export const tool$clientsVerify: ToolDefinition<typeof args> = {
  name: 'clients-verify',
  description: `Verify a client

Verifies the client in the provided token`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await clientsVerify(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
