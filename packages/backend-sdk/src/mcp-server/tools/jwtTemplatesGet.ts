/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { jwtTemplatesGet } from '../../funcs/jwtTemplatesGet.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.GetJWTTemplateRequest$inboundSchema,
};

export const tool$jwtTemplatesGet: ToolDefinition<typeof args> = {
  name: 'jwt-templates-get',
  description: `Retrieve a template

Retrieve the details of a given JWT template`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await jwtTemplatesGet(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
