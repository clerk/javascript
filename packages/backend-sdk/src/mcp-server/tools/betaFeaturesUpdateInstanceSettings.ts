/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { betaFeaturesUpdateInstanceSettings } from '../../funcs/betaFeaturesUpdateInstanceSettings.js';
import * as operations from '../../models/operations/index.js';
import { formatResult, ToolDefinition } from '../tools.js';

const args = {
  request: operations.UpdateInstanceAuthConfigRequestBody$inboundSchema.optional(),
};

export const tool$betaFeaturesUpdateInstanceSettings: ToolDefinition<typeof args> = {
  name: 'beta-features-update-instance-settings',
  description: `Update instance settings

Updates the settings of an instance`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await betaFeaturesUpdateInstanceSettings(client, args.request, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: 'text', text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
