import { createDebugLogger } from '../../utils/debugLogger';
import { logFormatter } from '../../utils/logFormatter';

export const ClerkProviderDebugLogger = createDebugLogger('<ClerkProvider/>', logFormatter)();
