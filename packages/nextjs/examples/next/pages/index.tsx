import type { NextPage } from 'next';
import Head from 'next/head';
import Image from 'next/image';
import React from 'react';

import styles from '../styles/Home.module.css';

const Home: NextPage = () => {
  const [responses, setResponse] = React.useState<Record<string, string>>({});

  const makeRequest = async (path: string) => {
    setResponse({
      ...responses,
      [path]: '// Loading...',
    });

    try {
      const res = await fetch(path);
      const body = await res.json();
      setResponse({
        ...responses,
        [path]: JSON.stringify(body, null, '  '),
      });
    } catch (e) {
      setResponse({ ...responses, [path]: '// There was an error with the request. Please contact support@clerk.com' });
    }
  };

  const getResponse = (path: string): string => responses[path] || '// Click above to run the request';

  return (
    <div className={styles.container}>
      <Head>
        <title>Create Next App</title>
        <meta
          name='description'
          content='Generated by create next app'
        />
        <link
          rel='icon'
          href='/favicon.ico'
        />
      </Head>

      <main className={styles.main}>
        <h1 className={styles.title}>
          Welcome to <a href='https://nextjs.org'>Next.js!</a>
        </h1>

        <p className={styles.description}>
          Get started by editing <code className={styles.code}>pages/index.tsx</code>
        </p>

        <div className={styles.grid}>
          <div className={styles.card}>
            <h2>withAuth &rarr;</h2>
            <p>
              <button onClick={() => makeRequest('/api/with-auth')}>Make request</button>
            </p>
            <pre>
              <code>{getResponse('/api/with-auth')}</code>
            </pre>
          </div>

          <div className={styles.card}>
            <h2>requireAuth &rarr;</h2>
            <button onClick={() => makeRequest('/api/require-auth')}>Make request</button>
            <pre>
              <code>{getResponse('/api/require-auth')}</code>
            </pre>
          </div>
        </div>
      </main>

      <footer className={styles.footer}>
        <a
          href='https://vercel.com?utm_source=create-next-app&utm_medium=default-template&utm_campaign=create-next-app'
          target='_blank'
          rel='noopener noreferrer'
        >
          Powered by{' '}
          <span className={styles.logo}>
            <Image
              src='/vercel.svg'
              alt='Vercel Logo'
              width={72}
              height={16}
            />
          </span>
        </a>
      </footer>
    </div>
  );
};

export default Home;
