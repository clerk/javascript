import { AccountEmails as Section } from '../../components/UserProfile/AccountSections';
import { createSection } from '../createSection';

export const AccountEmails = createSection('AccountEmails', Section);
