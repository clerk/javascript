{
  "extends": ["//"],
  "tasks": {
    "test": {
      "dependsOn": ["build"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "!**/__snapshots__/**",
        "!CHANGELOG.md",
        "!coverage/**",
        "!build/**",
        "!examples/**",
        "!node_modules/**"
      ],
      "outputLogs": "new-only",
      "outputs": ["build"]
    }
  }
}
