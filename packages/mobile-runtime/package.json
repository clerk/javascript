{
  "name": "@clerk/mobile-runtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:attached": "node build-attached.mjs",
    "check:attached": "node build-attached.mjs --check",
    "check:reproducibility": "node check-reproducibility.mjs",
    "test": "node --no-opt --test test/*.test.mjs"
  },
  "dependencies": {
    "@clerk/shared": "workspace:^",
    "core-js": "catalog:repo"
  },
  "devDependencies": {
    "rolldown": "catalog:repo"
  }
}
