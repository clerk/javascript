import * as OrganizationBillingPaymentsSectionStories from './organization-billing-payments-section.stories';

# Organization Billing Payments Section

The payment-attempts list of the organization billing panel — a table of payment attempts that navigates to a payment attempt's detail screen on row select. It renders nothing until the first page resolves.

<Story
  name='Default'
  storyModule={OrganizationBillingPaymentsSectionStories}
  composition={[
    {
      name: 'OrganizationBillingPanel',
      href: '/organization/organization-billing-panel',
      layer: 'Organization',
    },
  ]}
/>
