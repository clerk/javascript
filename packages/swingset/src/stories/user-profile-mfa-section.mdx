import * as Stories from './user-profile-mfa-section.stories';

# UserProfileMfaSection

Two-step verification methods composed with the shared Security list treatment.

<Story
  name='Default'
  storyModule={Stories}
  composition={[{ name: 'Section', href: '/components/section', layer: 'Components' }]}
/>

<Story
  name='Empty'
  storyModule={Stories}
  composition={[{ name: 'Section', href: '/components/section', layer: 'Components' }]}
/>
