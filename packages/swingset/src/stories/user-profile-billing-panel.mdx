import * as Stories from './user-profile-billing-panel.stories';

# UserProfileBillingPanel

Subscription, payment methods, and an inline billing history table composed without the surrounding navigation shell.

<Story
  name='Default'
  storyModule={Stories}
  composition={[{ name: 'Section', href: '/components/section', layer: 'Components' }]}
/>
