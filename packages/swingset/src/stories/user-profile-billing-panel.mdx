import * as Stories from './user-profile-billing-panel.stories';

# UserProfileBillingPanel

Subscription and payment methods composed without the surrounding navigation shell. Billing history is intentionally deferred.

<Story
  name='Default'
  storyModule={Stories}
  composition={[{ name: 'Section', href: '/components/section', layer: 'Components' }]}
/>
