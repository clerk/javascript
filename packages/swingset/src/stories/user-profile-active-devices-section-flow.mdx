import * as Stories from './user-profile-active-devices-section-flow.stories';

# Active devices section flow

Device details, single-device sign out, and the atomic bulk action through a section-local simulated backend and reverification gate.

<Story name='Default' storyModule={Stories} />

## States

Pure snapshots of every active-device dialog state, with no backend behind them.

<Story name='States' storyModule={Stories} />
