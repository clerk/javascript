import * as Stories from './user-profile-sign-out-all-devices-dialog.stories';

# UserProfileSignOutAllDevicesDialog

Confirms before ending every other active device session.

<Story
  name='Default'
  storyModule={Stories}
  composition={[{ name: 'AlertDialog', href: '/components/alert-dialog', layer: 'Components' }]}
/>
