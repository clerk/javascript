import * as IconStories from './icon.stories';

# Icon

Icon renders a named glyph from Mosaic's icon set. It sizes through the `size` variant. Monochrome glyphs inherit `currentColor`; device illustrations keep their Figma palettes. Any glyph can be swapped per name through the `icons` prop on `MosaicProvider`. Mosaic's styling applies to an override just like the built-in glyph — so swapped icons stay visually consistent — and the override also carries the `.cl-icon` class and `data-size` attribute for targeting.

Set `placement` when the icon sits beside text inside a container. It adds no styling of its own; it reflects `data-icon="inline-start"` or `data-icon="inline-end"` so the container can react — a `Button`, for example, tightens its padding on the side the icon sits with `:has([data-icon='inline-end'])`. The values are the CSS logical directions, so they follow the writing mode rather than naming a physical edge. Leave it unset for a standalone icon.

## Playground

<Preview
  name='Default'
  storyModule={IconStories}
/>

## Props

<PropTable
  meta={IconStories.meta}
  extra={[
    { name: 'name', type: 'IconName' },
    { name: 'placement', type: "'inline-start' | 'inline-end'" },
  ]}
/>

## Usage

<Usage
  component='Icon'
  module='@clerk/mosaic/components/icon'
  props={{ name: 'chevron-right' }}
/>

---

## Examples

### Sizes

<Story
  name='Sizes'
  storyModule={IconStories}
/>

### Icons

The [Figma icon set](https://www.figma.com/design/yt5qKlfNyIIdaKaMoav1v2/Clerk-Mosaic-2.0?node-id=8004-6488), alongside the dedicated security glyphs.

<Story
  name='Names'
  storyModule={IconStories}
  showCode={false}
/>

### Device illustrations

The filled phone and laptop from [Figma](https://www.figma.com/design/yt5qKlfNyIIdaKaMoav1v2/Clerk-Mosaic-2.0?node-id=163-1747) use the same Icon component and per-name overrides.

<Story
  name='DeviceIllustrations'
  storyModule={IconStories}
  showCode={false}
/>

### Country flags

[Figma flag artwork](https://www.figma.com/design/yt5qKlfNyIIdaKaMoav1v2/Clerk-Mosaic-2.0?node-id=10800-84326) for the same 238 countries supported by the legacy phone input, labeled by country code. `getCountryFlag(iso)` returns the flag glyph used by the phone input; unknown codes return a globe.

<Story
  name='Flags'
  storyModule={IconStories}
  showCode={false}
/>

### Overriding a glyph

Pass `icons` to `MosaicProvider` to replace a glyph by name. The override is a plain element of any type, not just an `svg`; Mosaic clones it with the resolved sizing `className` and `data-size`, keeping any class the element already had. The replacement only supplies its own content.

<Story
  name='Override'
  storyModule={IconStories}
/>
