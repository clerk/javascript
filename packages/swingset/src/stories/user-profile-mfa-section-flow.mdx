import * as Stories from './user-profile-mfa-section-flow.stories';

# MFA section flow

Phone, authenticator, removal, and backup-code journeys through a section-local simulated backend and reverification gate.

<Story name='Default' storyModule={Stories} />

## States

Pure snapshots of every MFA-dialog state, with no backend behind them.

<Story name='States' storyModule={Stories} />
