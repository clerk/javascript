import * as Stories from './user-profile-password-section-flow.stories';

# Password section flow

The password section and every dialog it can raise, driven by its own simulated backend and reverification gate.

<Story name='Default' storyModule={Stories} />

## States

Pure snapshots of every password-dialog state, with no backend behind them.

<Story name='States' storyModule={Stories} />
