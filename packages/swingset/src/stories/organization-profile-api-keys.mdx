import * as OrganizationProfileApiKeysStories from './organization-profile-api-keys.stories';

# Organization Profile API Keys

The API Keys tab panel of the Organization Profile — searchable key table with Date, Last used, and Expires columns.

<Story
  name='Default'
  storyModule={OrganizationProfileApiKeysStories}
  composition={[
    { name: 'Button', href: '/components/button', layer: 'Components' },
  ]}
/>
