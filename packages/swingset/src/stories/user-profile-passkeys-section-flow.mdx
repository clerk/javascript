import * as Stories from './user-profile-passkeys-section-flow.stories';

# Passkeys section flow

Add, rename, and remove passkeys through a section-local simulated backend and reverification gate.

<Story name='Default' storyModule={Stories} />

## States

Pure snapshots of every passkey-dialog state, with no backend behind them.

<Story name='States' storyModule={Stories} />
