import * as Stories from './user-profile-delete-section-flow.stories';

# Delete section flow

The destructive account-deletion journey through its own simulated backend and reverification gate.

<Story name='Default' storyModule={Stories} />

## States

Pure snapshots of every delete-account dialog state, with no backend behind them.

<Story name='States' storyModule={Stories} />
