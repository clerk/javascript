import { ReverificationOTP } from '@clerk/ui/mosaic/blocks/reverification';
import React from 'react';

import type { StoryMeta } from '@/lib/types';

import { ReverificationStoryCard } from './reverification-story-card';

export { default as __source } from './reverification-otp.stories?raw';

export const meta: StoryMeta = {
  group: 'Blocks',
  title: 'ReverificationOTP',
  label: 'OTP',
  navigation: { category: 'Reverification' },
  source: 'packages/ui/src/mosaic/blocks/reverification/reverification-otp.tsx',
};

export function Default() {
  const [value, setValue] = React.useState('');

  return (
    <ReverificationStoryCard>
      <ReverificationOTP
        messages={{
          title: 'Verification required',
          description: 'Enter the code sent to your phone to continue',
          fieldLabel: 'Verification code',
          secondaryActionLabel: 'Cancel',
          primaryActionLabel: 'Continue',
          pendingLabel: 'Verifying',
        }}
        value={value}
        resend={{
          label: 'Didn’t receive a code? Resend',
          onClick: () => setValue(''),
        }}
        onValueChange={setValue}
        onSubmit={() => undefined}
        onCancel={() => setValue('')}
      />
    </ReverificationStoryCard>
  );
}

export function Authenticator() {
  const [value, setValue] = React.useState('');

  return (
    <ReverificationStoryCard>
      <ReverificationOTP
        messages={{
          title: 'Verification required',
          description: 'Enter the code generated by your authenticator app to continue',
          fieldLabel: 'Verification code',
          secondaryActionLabel: 'Cancel',
          primaryActionLabel: 'Continue',
          pendingLabel: 'Verifying',
        }}
        value={value}
        onValueChange={setValue}
        onSubmit={() => undefined}
        onCancel={() => setValue('')}
      />
    </ReverificationStoryCard>
  );
}
