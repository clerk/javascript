import * as Stories from './user-profile-passkeys-section.stories';

# UserProfilePasskeysSection

Passkey management composed with the shared Security list treatment.

<Story
  name='Default'
  storyModule={Stories}
  composition={[{ name: 'Section', href: '/components/section', layer: 'Components' }]}
/>

<Story
  name='Empty'
  storyModule={Stories}
  composition={[{ name: 'Section', href: '/components/section', layer: 'Components' }]}
/>
