import * as Stories from './user-profile-passkeys-section.stories';

# UserProfilePasskeysSection

Manage passkey names and confirm removal. These examples mock creation; browser and authenticator interactions belong to the controller.

## Playground

<Story name='Default' storyModule={Stories} composition={[
  { name: 'Section', href: '/components/section', layer: 'Components' },
  { name: 'Button', href: '/components/button', layer: 'Components' },
  { name: 'Menu', href: '/components/menu', layer: 'Components' },
  { name: 'Dialog', href: '/components/dialog', layer: 'Components' },
  { name: 'Confirmation', href: '/blocks/confirmation', layer: 'Blocks' },
]} />

## Props

| Prop           | Type                                                  | Default      | Description                                                      |
| -------------- | ----------------------------------------------------- | ------------ | ---------------------------------------------------------------- |
| `passkeys`     | `UserProfilePasskey[]`                                | — (required) | Names and formatted creation/last-used labels.                   |
| `sectionTitle` | `string`                                              | —            | Optional surrounding section heading.                            |
| `onAdd`        | `() => void`                                          | —            | Shows Add when supplied.                                         |
| `addError`     | `string`                                              | —            | Inline creation error.                                           |
| `onRename`     | `(id: string, name: string) => void \| Promise<void>` | —            | Resolve to close; reject with an Error to display the failure.   |
| `onRemove`     | `(id: string) => void \| Promise<void>`               | —            | Shared confirmation awaits removal and displays rejected errors. |

## Usage

### Visibility

The caller hides the entire section when passkeys are disabled for the instance or identification creation is disallowed, even if the user already has passkeys. Set `passkeysVisible={false}` on the Security panel to omit it. The standalone section always renders when mounted and does not read instance settings.

On a satellite domain, an otherwise visible section remains visible even when empty; only Add is unavailable. Represent that by omitting `onAdd`, without hiding the section.

### No passkeys yet

The section remains visible after removing the final passkey. Add remains available when its callback is supplied.

<Story name='Empty' storyModule={Stories} />

### Creation unavailable

Rename and remove existing passkeys. Removing the final passkey leaves the empty section visible, matching a satellite domain where creation is unavailable.

<Story name='CreationUnavailable' storyModule={Stories} />

### Recoverable errors

The first Add, Rename, and Remove attempt fails. Retry completes the action; saving and removal use a short simulated delay.

<Story name='RecoverableErrors' storyModule={Stories} />
