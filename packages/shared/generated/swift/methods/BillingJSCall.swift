// Generated by packages/shared/scripts/generate-swift-models.ts. Do not edit.

import Foundation

public enum BillingJSCall: ClerkJSCallable {
  case getCreditBalance(GetCreditBalanceParams)
  case getCreditHistory(GetCreditHistoryParams)
  case getPaymentAttempt(GetPaymentAttemptParams)
  case getPaymentAttempts(GetPaymentAttemptsParams)
  case getPlan(GetPlanParams)
  case getPlans(GetPlansParams?)
  case getStatement(GetStatementParams)
  case getStatements(GetStatementsParams)
  case getSubscription(GetSubscriptionParams)
  case startCheckout(CreateCheckoutParams)
  case updateCheckout(UpdateCheckoutParams)

  public var jsMethod: String {
    switch self {
    case .getCreditBalance:
      return BillingJSMethod.getCreditBalance.rawValue
    case .getCreditHistory:
      return BillingJSMethod.getCreditHistory.rawValue
    case .getPaymentAttempt:
      return BillingJSMethod.getPaymentAttempt.rawValue
    case .getPaymentAttempts:
      return BillingJSMethod.getPaymentAttempts.rawValue
    case .getPlan:
      return BillingJSMethod.getPlan.rawValue
    case .getPlans:
      return BillingJSMethod.getPlans.rawValue
    case .getStatement:
      return BillingJSMethod.getStatement.rawValue
    case .getStatements:
      return BillingJSMethod.getStatements.rawValue
    case .getSubscription:
      return BillingJSMethod.getSubscription.rawValue
    case .startCheckout:
      return BillingJSMethod.startCheckout.rawValue
    case .updateCheckout:
      return BillingJSMethod.updateCheckout.rawValue
    }
  }

  public func jsArguments() throws -> [JSONValue] {
    switch self {
    case .getCreditBalance(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .getCreditHistory(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .getPaymentAttempt(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .getPaymentAttempts(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .getPlan(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .getPlans(let params):
      var args: [JSONValue] = []
      if let params { args.append(try JSONValue(encoding: params)) }
      return args
    case .getStatement(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .getStatements(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .getSubscription(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .startCheckout(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .updateCheckout(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    }
  }
}
