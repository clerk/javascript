// Generated by packages/shared/scripts/generate-swift-models.ts. Do not edit.

import Foundation

public enum OrganizationJSCall: ClerkJSCallable {
  case addMember(AddMemberParams)
  case addPaymentMethod(AddPaymentMethodParams)
  case attemptOwnershipVerification(domainIds: [String])
  case createDomain(domainName: String, params: OrganizationCreateDomainParams?)
  case createEnterpriseConnection(CreateOrganizationEnterpriseConnectionParams)
  case createEnterpriseConnectionTestRun(enterpriseConnectionId: String)
  case deleteEnterpriseConnection(enterpriseConnectionId: String)
  case destroy
  case getDomain(OrganizationGetDomain_0)
  case getDomains(GetDomainsParams?)
  case getEnterpriseConnections(GetEnterpriseConnectionsParams?)
  case getEnterpriseConnectionTestRuns(enterpriseConnectionId: String, params: GetEnterpriseConnectionTestRunsParams?)
  case getInvitations(GetInvitationsParams?)
  case getMembershipRequests(GetMembershipRequestParams?)
  case getMemberships(GetMembersParams?)
  case getPaymentMethods(GetPaymentMethodsParams?)
  case getRoles(GetRolesParams?)
  case initializePaymentMethod(InitializePaymentMethodParams)
  case inviteMember(InviteMemberParams)
  case inviteMembers(InviteMembersParams)
  case prepareOwnershipVerification(domainIds: [String])
  case reload(ClerkResourceReloadParams?)
  case removeMember(userId: String)
  case setLogo(SetOrganizationLogoParams)
  case update(UpdateOrganizationParams)
  case updateEnterpriseConnection(enterpriseConnectionId: String, params: UpdateOrganizationEnterpriseConnectionParams)
  case updateMember(UpdateMembershipParams)

  public var jsMethod: String {
    switch self {
    case .addMember:
      return OrganizationJSMethod.addMember.rawValue
    case .addPaymentMethod:
      return OrganizationJSMethod.addPaymentMethod.rawValue
    case .attemptOwnershipVerification:
      return OrganizationJSMethod.attemptOwnershipVerification.rawValue
    case .createDomain:
      return OrganizationJSMethod.createDomain.rawValue
    case .createEnterpriseConnection:
      return OrganizationJSMethod.createEnterpriseConnection.rawValue
    case .createEnterpriseConnectionTestRun:
      return OrganizationJSMethod.createEnterpriseConnectionTestRun.rawValue
    case .deleteEnterpriseConnection:
      return OrganizationJSMethod.deleteEnterpriseConnection.rawValue
    case .destroy:
      return OrganizationJSMethod.destroy.rawValue
    case .getDomain:
      return OrganizationJSMethod.getDomain.rawValue
    case .getDomains:
      return OrganizationJSMethod.getDomains.rawValue
    case .getEnterpriseConnections:
      return OrganizationJSMethod.getEnterpriseConnections.rawValue
    case .getEnterpriseConnectionTestRuns:
      return OrganizationJSMethod.getEnterpriseConnectionTestRuns.rawValue
    case .getInvitations:
      return OrganizationJSMethod.getInvitations.rawValue
    case .getMembershipRequests:
      return OrganizationJSMethod.getMembershipRequests.rawValue
    case .getMemberships:
      return OrganizationJSMethod.getMemberships.rawValue
    case .getPaymentMethods:
      return OrganizationJSMethod.getPaymentMethods.rawValue
    case .getRoles:
      return OrganizationJSMethod.getRoles.rawValue
    case .initializePaymentMethod:
      return OrganizationJSMethod.initializePaymentMethod.rawValue
    case .inviteMember:
      return OrganizationJSMethod.inviteMember.rawValue
    case .inviteMembers:
      return OrganizationJSMethod.inviteMembers.rawValue
    case .prepareOwnershipVerification:
      return OrganizationJSMethod.prepareOwnershipVerification.rawValue
    case .reload:
      return OrganizationJSMethod.reload.rawValue
    case .removeMember:
      return OrganizationJSMethod.removeMember.rawValue
    case .setLogo:
      return OrganizationJSMethod.setLogo.rawValue
    case .update:
      return OrganizationJSMethod.update.rawValue
    case .updateEnterpriseConnection:
      return OrganizationJSMethod.updateEnterpriseConnection.rawValue
    case .updateMember:
      return OrganizationJSMethod.updateMember.rawValue
    }
  }

  public func jsArguments() throws -> [JSONValue] {
    switch self {
    case .addMember(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .addPaymentMethod(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .attemptOwnershipVerification(let domainIds):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: domainIds))
      return args
    case .createDomain(let domainName, let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: domainName))
      if let params { args.append(try JSONValue(encoding: params)) }
      return args
    case .createEnterpriseConnection(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .createEnterpriseConnectionTestRun(let enterpriseConnectionId):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: enterpriseConnectionId))
      return args
    case .deleteEnterpriseConnection(let enterpriseConnectionId):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: enterpriseConnectionId))
      return args
    case .destroy:
      return []
    case .getDomain(let __0):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: __0))
      return args
    case .getDomains(let params):
      var args: [JSONValue] = []
      if let params { args.append(try JSONValue(encoding: params)) }
      return args
    case .getEnterpriseConnections(let params):
      var args: [JSONValue] = []
      if let params { args.append(try JSONValue(encoding: params)) }
      return args
    case .getEnterpriseConnectionTestRuns(let enterpriseConnectionId, let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: enterpriseConnectionId))
      if let params { args.append(try JSONValue(encoding: params)) }
      return args
    case .getInvitations(let params):
      var args: [JSONValue] = []
      if let params { args.append(try JSONValue(encoding: params)) }
      return args
    case .getMembershipRequests(let params):
      var args: [JSONValue] = []
      if let params { args.append(try JSONValue(encoding: params)) }
      return args
    case .getMemberships(let params):
      var args: [JSONValue] = []
      if let params { args.append(try JSONValue(encoding: params)) }
      return args
    case .getPaymentMethods(let params):
      var args: [JSONValue] = []
      if let params { args.append(try JSONValue(encoding: params)) }
      return args
    case .getRoles(let params):
      var args: [JSONValue] = []
      if let params { args.append(try JSONValue(encoding: params)) }
      return args
    case .initializePaymentMethod(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .inviteMember(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .inviteMembers(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .prepareOwnershipVerification(let domainIds):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: domainIds))
      return args
    case .reload(let p):
      var args: [JSONValue] = []
      if let p { args.append(try JSONValue(encoding: p)) }
      return args
    case .removeMember(let userId):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: userId))
      return args
    case .setLogo(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .update(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .updateEnterpriseConnection(let enterpriseConnectionId, let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: enterpriseConnectionId))
      args.append(try JSONValue(encoding: params))
      return args
    case .updateMember(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    }
  }
}
