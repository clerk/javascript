// Generated by packages/shared/scripts/generate-swift-models.ts. Do not edit.

import Foundation

public enum SessionJSCall: ClerkJSCallable {
  case attemptFirstFactorVerification(SessionVerifyAttemptFirstFactorParams)
  case attemptSecondFactorVerification(SessionVerifyAttemptSecondFactorParams)
  case end
  case getToken(GetTokenOptions?)
  case prepareFirstFactorVerification(SessionVerifyPrepareFirstFactorParams)
  case prepareSecondFactorVerification(PhoneCodeSecondFactorConfig)
  case reload(ClerkResourceReloadParams?)
  case remove
  case startVerification(SessionVerifyCreateParams)
  case touch(SessionTouchParams?)
  case verifyWithPasskey

  public var jsMethod: String {
    switch self {
    case .attemptFirstFactorVerification:
      return SessionJSMethod.attemptFirstFactorVerification.rawValue
    case .attemptSecondFactorVerification:
      return SessionJSMethod.attemptSecondFactorVerification.rawValue
    case .end:
      return SessionJSMethod.end.rawValue
    case .getToken:
      return SessionJSMethod.getToken.rawValue
    case .prepareFirstFactorVerification:
      return SessionJSMethod.prepareFirstFactorVerification.rawValue
    case .prepareSecondFactorVerification:
      return SessionJSMethod.prepareSecondFactorVerification.rawValue
    case .reload:
      return SessionJSMethod.reload.rawValue
    case .remove:
      return SessionJSMethod.remove.rawValue
    case .startVerification:
      return SessionJSMethod.startVerification.rawValue
    case .touch:
      return SessionJSMethod.touch.rawValue
    case .verifyWithPasskey:
      return SessionJSMethod.verifyWithPasskey.rawValue
    }
  }

  public func jsArguments() throws -> [JSONValue] {
    switch self {
    case .attemptFirstFactorVerification(let attemptFactor):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: attemptFactor))
      return args
    case .attemptSecondFactorVerification(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .end:
      return []
    case .getToken(let options):
      var args: [JSONValue] = []
      if let options { args.append(try JSONValue(encoding: options)) }
      return args
    case .prepareFirstFactorVerification(let factor):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: factor))
      return args
    case .prepareSecondFactorVerification(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .reload(let p):
      var args: [JSONValue] = []
      if let p { args.append(try JSONValue(encoding: p)) }
      return args
    case .remove:
      return []
    case .startVerification(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .touch(let params):
      var args: [JSONValue] = []
      if let params { args.append(try JSONValue(encoding: params)) }
      return args
    case .verifyWithPasskey:
      return []
    }
  }
}
