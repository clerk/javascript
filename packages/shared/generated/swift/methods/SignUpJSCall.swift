// Generated by packages/shared/scripts/generate-swift-models.ts. Do not edit.

import Foundation

public enum SignUpJSCall: ClerkJSCallable {
  case attemptEmailAddressVerification(AttemptEmailAddressVerificationParams)
  case attemptPhoneNumberVerification(AttemptPhoneNumberVerificationParams)
  case attemptVerification(AttemptVerificationParams)
  case create(SignUpCreateParams)
  case prepareEmailAddressVerification(SignUpPrepareEmailAddressVerificationParams?)
  case preparePhoneNumberVerification(PreparePhoneNumberVerificationParams?)
  case prepareVerification(PrepareVerificationParams)
  case reload(ClerkResourceReloadParams?)
  case submitProtectCheck(SignInSubmitProtectCheckParams)
  case update(SignUpCreateParams)
  case upsert(SignUpCreateParams)

  public var jsMethod: String {
    switch self {
    case .attemptEmailAddressVerification:
      return SignUpJSMethod.attemptEmailAddressVerification.rawValue
    case .attemptPhoneNumberVerification:
      return SignUpJSMethod.attemptPhoneNumberVerification.rawValue
    case .attemptVerification:
      return SignUpJSMethod.attemptVerification.rawValue
    case .create:
      return SignUpJSMethod.create.rawValue
    case .prepareEmailAddressVerification:
      return SignUpJSMethod.prepareEmailAddressVerification.rawValue
    case .preparePhoneNumberVerification:
      return SignUpJSMethod.preparePhoneNumberVerification.rawValue
    case .prepareVerification:
      return SignUpJSMethod.prepareVerification.rawValue
    case .reload:
      return SignUpJSMethod.reload.rawValue
    case .submitProtectCheck:
      return SignUpJSMethod.submitProtectCheck.rawValue
    case .update:
      return SignUpJSMethod.update.rawValue
    case .upsert:
      return SignUpJSMethod.upsert.rawValue
    }
  }

  public func jsArguments() throws -> [JSONValue] {
    switch self {
    case .attemptEmailAddressVerification(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .attemptPhoneNumberVerification(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .attemptVerification(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .create(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .prepareEmailAddressVerification(let params):
      var args: [JSONValue] = []
      if let params { args.append(try JSONValue(encoding: params)) }
      return args
    case .preparePhoneNumberVerification(let params):
      var args: [JSONValue] = []
      if let params { args.append(try JSONValue(encoding: params)) }
      return args
    case .prepareVerification(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .reload(let p):
      var args: [JSONValue] = []
      if let p { args.append(try JSONValue(encoding: p)) }
      return args
    case .submitProtectCheck(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .update(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    case .upsert(let params):
      var args: [JSONValue] = []
      args.append(try JSONValue(encoding: params))
      return args
    }
  }
}
