{
  "extends": ["//"],
  "pipeline": {
    "build": {
      "inputs": [
        "src/**",
        "tsup.config.ts",
        "tsconfig.json",
        "!**/**/*.test.*",
        "!**/test/**",
        "!**/__mocks__/**",
        "!**/__snapshots__/**",
        "!coverage/**"
      ]
    },
    "test": {
      "inputs": ["src/**", "jest.*", "tsup.config.ts", "tsconfig.json", "!**/__snapshots__/**", "!coverage/**"]
    }
  }
}
