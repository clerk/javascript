/**
 * Re-export all shared Clerk types for convenient access via @clerk/fastify/types
 */
export type * from '@clerk/shared/types';

/**
 * Fastify-specific types
 */
export type { ClerkFastifyOptions } from '../types';
