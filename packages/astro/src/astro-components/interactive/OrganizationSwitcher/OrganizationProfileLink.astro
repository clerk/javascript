---
import ProfilePageRenderer from './ProfilePageRenderer.astro'

interface Props {
  url: string
  label: string
}

const { url, label } = Astro.props
---

<ProfilePageRenderer label={label} url={url} type="link">
    <slot name="label-icon" slot="label-icon" />
</ProfilePageRenderer>
