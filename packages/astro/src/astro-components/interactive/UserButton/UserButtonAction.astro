---
import MenuItemRenderer from './MenuItemRenderer.astro';

interface Props {
  label: string
  open?: string
}

const { label, open } = Astro.props
---

<MenuItemRenderer label={label} open={open}>
  <slot name="label-icon" slot="label-icon" />
</MenuItemRenderer>
