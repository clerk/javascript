---
import SignedInSSR from './SignedInSSR.astro';
import SignedInCSR from './SignedInCSR.astro';

type Props = {
  client?: boolean;
}

const { client = false } = Astro.props;
---

{client ? <SignedInCSR><slot /></SignedInCSR> : <SignedInSSR><slot /></SignedInSSR> }
