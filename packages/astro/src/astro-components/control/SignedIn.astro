---
import SignedInSSR from "./SignedInSSR.astro";
import SignedInCSR from "./SignedInCSR.astro";
// @ts-expect-error: Fix types later
import config from "virtual:astro/config";

const SignedInComponent = config.output === 'static' ? SignedInCSR : SignedInSSR;
---

<SignedInComponent>
  <slot />
</SignedInComponent>
