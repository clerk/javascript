---
import SignedInCSR from './SignedInCSR.astro';
import SignedInSSR from './SignedInSSR.astro';
// @ts-expect-error: Virtual module
import { isStaticOutput } from 'virtual:@clerk/astro/config';

type Props = {
  isStatic?: boolean
}

const SignedInComponent = isStaticOutput(Astro.props.isStatic) ? SignedInCSR : SignedInSSR;
---

<SignedInComponent>
  <slot />
</SignedInComponent>
