---
import SignedInSSR from "./SignedInSSR.astro";
import SignedInCSR from "./SignedInCSR.astro";

type Props = {
  client?: boolean;
};

const SignedInComponent = Astro.props.client ? SignedInCSR : SignedInSSR;
---

<SignedInComponent>
  <slot />
</SignedInComponent>
