---
import SignedOutCSR from './SignedOutCSR.astro';
import SignedOutSSR from './SignedOutSSR.astro';
// @ts-expect-error: Virtual module
import { isStaticOutput } from 'virtual:@clerk/astro/config';

type Props = {
  isStatic?: boolean
}

const SignedOutComponent = isStaticOutput(Astro.props.isStatic) ? SignedOutCSR : SignedOutSSR;
---

<SignedOutComponent>
  <slot />
</SignedOutComponent>
