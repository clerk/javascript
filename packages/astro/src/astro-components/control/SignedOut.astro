---
import SignedOutSSR from './SignedOutSSR.astro';
import SignedOutCSR from './SignedOutCSR.astro';

type Props = {
  client?: boolean;
}

const { client = false } = Astro.props;
---

{client ? <SignedOutCSR><slot /></SignedOutCSR> : <SignedOutSSR><slot /></SignedOutSSR> }
