---
import SignedOutSSR from './SignedOutSSR.astro';
import SignedOutCSR from './SignedOutCSR.astro';

type Props = {
  client?: boolean;
}

const SignedOutComponent = Astro.props.client ? SignedOutCSR : SignedOutSSR;
---

<SignedOutComponent>
  <slot />
</SignedOutComponent>
