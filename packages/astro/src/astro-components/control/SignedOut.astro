---
import SignedOutSSR from './SignedOutSSR.astro';
import SignedOutCSR from './SignedOutCSR.astro';
// @ts-expect-error: Fix types later
import config from "virtual:astro/config";

const SignedOutComponent = config.output === 'static' ? SignedOutCSR : SignedOutSSR;
---

<SignedOutComponent>
  <slot />
</SignedOutComponent>
