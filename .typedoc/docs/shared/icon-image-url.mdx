Returns the URL for a static image
using the new img.clerk.com service

## Parameters

| Parameter | Type                         |
| --------- | ---------------------------- |
| `id`      | `string`                     |
| `format`  | <code>"svg" \| "jpeg"</code> |

## Returns

`string`
