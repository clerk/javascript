Convert words to a sentence.

## Parameters

| Parameter | Type                  | Description                     |
| --------- | --------------------- | ------------------------------- |
| `items`   | <code>string[]</code> | An array of words to be joined. |

## Returns

`string` — A string with the items joined by a comma and the last item joined by ", or".
