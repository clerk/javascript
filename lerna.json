{
  "version": "independent",
  "packages": ["packages/*"],
  "command": {
    "version": {
      "message": "chore(release): Publish",
      "allowBranch": ["main", "next", "staging", "release"],
      "conventionalCommits": true
    },
    "publish": {
      "allowBranch": ["main", "next", "staging", "release"]
    }
  },
  "changelogPreset": {
    "name": "conventionalcommits"
  }
}
